## 1. Implementation
- [ ] 1.1 In `kanata_mouse_bridge.ahk`, update the `*F23::` hotkey (Halmak) to play `SoundBeep` once at 750Hz.
- [ ] 1.2 In `kanata_mouse_bridge.ahk`, update the `*F24::` hotkey (Navigation) to loop `SoundBeep` twice at 1200Hz with a short `Sleep` between.
- [ ] 1.3 In `kanata_mouse_bridge.ahk`, update the `^!F22::` hotkey (QWERTY) to loop `SoundBeep` three times at 400Hz with a short `Sleep` between.
