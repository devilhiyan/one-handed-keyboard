# Tasks

- [ ] Remove diagnostic `ToolTip` from `ProcessMovement` in `kanata_mouse_bridge.ahk`.
- [ ] In `kanata_mouse_bridge.ahk`, locate `#HotIf NavMode && !MouseMode`.
- [ ] Ensure `Shift` (mapped via F18 from Kanata) passes through as `Shift` in this mode.
- [ ] Ensure `Space & F18` (Space + Shift) maps to `Click "Right"`.
