# Tasks

- [ ] Add debug tooltips to `Capslock` and `Shift` handlers in `kanata_mouse_bridge.ahk` to verify event reception.
- [ ] Check if `kanata.kbd` maps CapsLock/Shift to special actions or passes them through.
- [ ] If keys are blocked, update `kanata.kbd` to map mouse-mode CapsLock to `f17` (Left Click) and Shift to `f18` (Right Click).
- [ ] Update `kanata_mouse_bridge.ahk` to listen for `f17` and `f18` for clicks.
- [ ] Verify Left Click drag behavior (hold F17 -> Hold Left Button).
