# Tasks

- [ ] Update `kanata/kanata.kbd`: Duplicate `ctrl-layer` into `ctrl-layer-halmak` and `ctrl-layer-qwer`.
- [ ] Update `kanata/kanata.kbd`: Define `lctl-mod-halmak` and `lctl-mod-qwer` aliases pointing to the respective layers.
- [ ] Update `kanata/kanata.kbd`: In `deflayer halmak`, replace `@lctl-mod` with `@lctl-mod-halmak`.
- [ ] Update `kanata/kanata.kbd`: In `deflayer qwer`, replace `@lctl-mod` with `@lctl-mod-qwer`.
- [ ] Update `kanata/kanata.kbd`: In `ctrl-layer-halmak`, map `grv` to `@tog-qwer` (which switches to qwer).
- [ ] Update `kanata/kanata.kbd`: In `ctrl-layer-qwer`, map `grv` to `@to-halmak` (which switches to halmak).
- [ ] Verify `to-halmak` and `tog-qwer` aliases exist and send the correct `f23` signals for the AHK bridge.
