# Tasks

- [ ] Update `kanata/kanata.kbd`: Apply `mouse-nav-chords` to keys `q`, `w`, `e`, `a`, `s`, `d` in the `mouse` layer.
- [ ] Update `kanata/kanata.kbd`: Ensure `(q w) f21` and `(w e) f22` are defined in `mouse-nav-chords`.
- [ ] Update `kanata/kanata_mouse_bridge.ahk`:
    - Keyboard Mode (`!MouseMode`): Map `f21` -> Home, `f22` -> End.
    - Mouse Mode (`MouseMode`): Map `f19` -> WheelUp, `f20` -> WheelDown.
- [ ] Verify functionality in both modes.