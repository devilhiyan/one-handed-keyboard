# Tasks

- [ ] Update `kanata/kanata_mouse_bridge.ahk`:
    - [ ] Change default `NavToggleKey` to `"Ctrl"`.
    - [ ] Implement `~Ctrl Up` hotkey to toggle `NavMode`.
    - [ ] Ensure notifications and beeps are consistent with the `LWin` toggle.
