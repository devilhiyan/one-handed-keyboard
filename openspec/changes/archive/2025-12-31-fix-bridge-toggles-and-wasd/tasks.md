# Tasks

- [ ] Update `kanata/kanata_mouse_bridge.ahk`:
    - [ ] Restore `*F24::` to toggle `NavMode`.
    - [ ] Remove `~Ctrl Up` and `NavToggleKey` logic (rely on Kanata's `F24`).
    - [ ] Update `ProcessMovement` to check appropriate keys based on `MouseMode`.
    - [ ] Define Hotkeys for `F13-F16` and `Up/Down/Left/Right` to handle movement or arrows.
    - [ ] Define Hotkeys for `F19-F20` and `Home/End` to handle wheels or Home/End.
    - [ ] Implement `Ctrl + Space` toggle for `MouseMode`.
