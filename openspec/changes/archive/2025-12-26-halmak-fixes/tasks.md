# Tasks

- [ ] Modify `Mirrored keyboard one hand.ahk2` @critical
    - [ ] Find the `Space Up::` hotkey (approx line 649) and replace its body with simple single-tap logic.
    - [ ] Find the `MirrorStyle == "Halmak"` section (approx line 1251).
    - [ ] Insert the `Capslock::` hotkey definition (mapping to `{Enter}`) to match other styles.
