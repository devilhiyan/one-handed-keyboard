# Tasks

- [ ] Modify `Mirrored keyboard one hand.ahk2` @critical
    - [ ] Locate the `PerformChord` function (approx line 60).
    - [ ] Change the timeout check from `50` to `90` ms.
    - [ ] Add a comment explaining the trade-off (easier nav vs rollover typing).
