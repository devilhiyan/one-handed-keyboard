# Tasks

- [ ] Update `^Space` hotkey in `Mirrored keyboard one hand.ahk2` to toggle between "Keyboard Navigation" and "Mouse Navigation" modes.
- [ ] Update notifications in `^Space` and `$LWin` to use the new mode names.
- [ ] Implement WASD Keyboard Navigation in `NavMode && !MouseMode` (Keyboard First).
    - [ ] `w`, `a`, `s`, `d` -> Arrow Keys.
    - [ ] `Space + w/a/s/d` -> Mouse Movement.
- [ ] Implement WASD Mouse Navigation in `NavMode && MouseMode` (Mouse First).
    - [ ] `w`, `a`, `s`, `d` -> Mouse Movement.
    - [ ] `Space + w/a/s/d` -> Arrow Keys.
- [ ] Update Chords for WASD style:
    - [ ] `q` + `w` (Halmak `cs`) -> Home.
    - [ ] `w` + `e` (Halmak `st`) -> End.
- [ ] Verify `NavStyle` defaults to "Old" (WASD) or is easily selectable.