# Tasks

- [ ] Modify `Mirrored keyboard one hand.ahk2` @critical
    - [ ] Update `RecordChordKeyDown` to record all keys and use `A_TickCount`.
    - [ ] Update `PerformChord` to use symmetric logic and the new timing windows.
    - [ ] Update `ChordHook` definition to ensure it doesn't suppress keys and uses level 1 priority.
