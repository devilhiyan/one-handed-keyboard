# Tasks

- [ ] Update `kanata/kanata.kbd`: Change `defchords` timeout from `100` to `50` for both `halmak-chords` and `mirror-chords`.
- [ ] Update `kanata/kanata.kbd`: Add `(l d) C-z` to `halmak-chords`.
- [ ] Update `kanata/kanata.kbd`: Add `(n m) C-y` to `halmak-chords`.
- [ ] Update `kanata/kanata.kbd`: Add mirrored equivalents for `(l d)` and `(n m)` to `mirror-chords` if applicable, or confirm if only needed on base layer. (Assuming base layer for now based on keys `l`, `d`, `n`, `m`).
- [ ] Ensure keys `l`, `d`, `n`, `m` are enabled in `deflayer halmak`.
