# Tasks

- [ ] Update `kanata/kanata.kbd` to modify `halmak-chords` with new backspace actions.
- [ ] Define `mirror-chords` in `kanata/kanata.kbd` for the mirror layer.
- [ ] Update `mirror` layer in `kanata/kanata.kbd` to use `mirror-chords` for keys `s`, `d`, `f`.
