# Tasks: Space + WASD = Arrow Keys

- [ ] **Step 1: Map Arrows in Mouse-Spc-Mod Layer**
    - Edit `kanata.kbd`.
    - In `deflayer mouse-spc-mod`:
        - Row 2 (Tab row): Change `w` position (3rd key) to `up`.
        - Row 3 (Caps row): Change `a` position (2nd key) to `left`.
        - Row 3 (Caps row): Change `s` position (3rd key) to `down`.
        - Row 3 (Caps row): Change `d` position (4th key) to `right`.
